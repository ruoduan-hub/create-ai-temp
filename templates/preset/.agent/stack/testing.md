# 前端测试增量规则

- 领域和应用逻辑使用 Vitest 单元测试，不启动 Next.js 运行时。
- React 组件使用 Testing Library，按角色、名称和可见文本查询。
- 不测试 React 内部状态或实现细节，关注用户能感知的行为。
- 测试环境入口为 `tests/setup.ts`，公共清理和匹配器统一配置在此。
- 路由、缓存和 Server Component 行为优先通过集成或端到端测试验证。
- 交互组件至少覆盖键盘可用性、加载、错误和禁用状态中的相关场景。
