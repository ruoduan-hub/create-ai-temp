# Zustand 增量规则

- Zustand 用于客户端跨组件状态，不用于替代服务端数据获取或领域模型。
- Store 作为展示层适配器放在 `presentation/stores`，可以调用纯领域函数。
- 按功能拆分 Store，避免单一全局 Store 汇集无关状态。
- 组件使用选择器订阅最小状态片段，避免不必要渲染。
- Action 使用业务语言命名，状态更新保持可预测，不从组件直接拼装复杂规则。
- 持久化前明确数据版本、迁移、隐私和服务端渲染水合策略。
- 测试之间必须重置共享 Store，避免用例相互污染。
