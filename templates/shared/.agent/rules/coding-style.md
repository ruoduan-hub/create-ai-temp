# 编码规范

- 命名表达意图：变量和函数使用 camelCase，类型与组件使用 PascalCase，常量使用 UPPER_SNAKE_CASE。
- 函数保持单一职责，参数较多时使用对象参数。
- 优先纯函数和显式数据流，避免隐式共享状态。
- 使用类型系统表达约束，不以 `any` 或无理由的类型断言绕过检查。
- 异步操作必须处理失败，并向调用方提供可行动的错误信息。
- 注释解释设计原因、业务约束或权衡，不复述代码。
- 不在生产代码中保留调试日志、废弃 API 或无用分支。
