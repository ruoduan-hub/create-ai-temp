# 可访问性规则

- 优先使用语义化 HTML 和原生交互元素。
- 所有功能必须支持键盘操作，并提供清晰的焦点状态。
- 图像提供符合语境的替代文本；装饰图像使用空替代文本。
- 表单控件必须有关联标签，错误信息可被辅助技术感知。
- 不只依靠颜色表达状态，正文颜色对比度至少达到 WCAG AA。
- 动画尊重减少动态效果偏好，异步状态通过合适的实时区域告知。
- UI 变更至少检查键盘路径、可访问名称和焦点管理。
