# 安全规则

- 密钥、令牌、密码只能来自安全的运行时配置，不得写入源码或示例值。
- 所有外部输入在边界处校验；输出到 HTML 前转义，确需插入 HTML 时使用可靠净化方案。
- 身份认证与权限检查在可信边界执行，客户端状态不能作为授权依据。
- Cookie、跨站请求和重定向需考虑 SameSite、CSRF 与开放重定向风险。
- 日志不得包含密码、令牌、完整身份证件或其他敏感信息。
- 引入依赖前确认来源、维护状态、许可证和必要性。
- 发现疑似泄漏时立即停止扩散，并说明需要轮换的凭据和影响范围。
